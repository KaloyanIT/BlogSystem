@model DetailedBlogPostViewModel

@{
    ViewData["Title"] = Model.Title;
}

<article>
    @Html.Raw(Model.Content)
    
    @(await Component.InvokeAsync<CommentsViewComponent>(new { id = Model.Id }))
</article>